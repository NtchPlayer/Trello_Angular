import { DeadlineHighlightDirective } from './deadline-highlight.directive';

describe('DeadlineHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new DeadlineHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
